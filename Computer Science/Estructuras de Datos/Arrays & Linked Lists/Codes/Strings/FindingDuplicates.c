#include <stdio.h>
#include <stdlib.h>

findDuplicatesComparingAll(Array *);
findDuplicatesHash(Array *);
findDuplicatesBits(Array *);

int main() {

    return 0;
}

findDuplicatesComparingAll(Array *);
findDuplicatesHash(Array *);
findDuplicatesBits(Array *);
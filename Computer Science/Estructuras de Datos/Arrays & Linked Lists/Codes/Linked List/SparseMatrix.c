#include <stdio.h>
#include <stdlib.h>

typedef struct Node {
    int column;
    int value;
    Node *next;
} Node;

void Create(Node **);

int main() {

    return 0;
}

void Create(Node **matrix) {

}